Only selective item can be move with dev to uat and prod
No duplication (set-based + single lookup)
Correct deployment (real content refresh) → uses DELETE + CREATE
Fabric-compatible → avoids PATCH completely
Folder replication (logical + path-based grouping)
Parallel deployment (fast)
Ready to use in Azure DevOps
Real notebook content upload 
Full Fabric folder hierarchy creation
Dependency-safe deployment
rollback ligic
